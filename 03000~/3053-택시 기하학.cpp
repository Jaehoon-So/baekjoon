#include <bits/stdc++.h>
#include <cmath>

using namespace std;

int main(){
    cout.sync_with_stdio(0);
    cout.precision(10);
    cin.tie(0);

    double r;
    cin >> r;

    printf("%.6f\n%.6f", (M_PI * r * r), (2.0 * r * r));
    return 0;
}