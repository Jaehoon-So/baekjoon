// A/B
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    cout.precision(10);
    long double a, b;
    cin >> a >> b;
    cout << (long double)a / b;

    return 0;
}
