#include <bits/stdc++.h>

using namespace std;
int cnt = 0;
int n;
const int MAXLEN = 100001;
int lionArray[MAXLEN][2];
bool promising(int num, int row){
    bool flag = true;
    if(lionArray[][])

}
int lion(int num, int row){
    if(!promising(num, row))
        return 0;
    if(num == n-1)
        return 1;

}

int main(){
    cout.sync_with_stdio(0);
    cin.tie(0);
    memset(lionArray, 0, sizeof(lionArray));
    cin >> n;

    cout << lion(0, 0) % 9901 << '\n';
    return 0;

}